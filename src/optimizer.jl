
function neg_sharpe_ratio(weights::AbstractVector, 
						  mean_returns::AbstractVector,
						  cov_matrix::AbstractVector,
						  risk_free_rate::Float64,
						  freq::Int64)
	nothing
end

function volatility(weights::AbstractVector, 
						  mean_returns::AbstractVector,
						  cov_matrix::AbstractVector,
						  risk_free_rate::Float64,
						  freq::Int64)
	nothing
end

function returns(weights::AbstractVector, 
						  mean_returns::AbstractVector,
						  cov_matrix::AbstractVector,
						  risk_free_rate::Float64,
						  freq::Int64)
	nothing
end

function EfficientFrontier(portfolio::Portfolio, 
						   min_function::Function)
	nothing
end

		                   
